# Build output
node_modules/
dist/
dist-test/
*.vsix

# Local review state: comment threads belong to whoever is reviewing,
# not to the repo. Ignored here because this project is its own test bed.
.review/

# Generated by "Set Up Claude Code Integration" — absolute paths to a
# particular machine's extension install, so never portable.
.mcp.json

# Per-machine Claude Code settings
.claude/settings.local.json
