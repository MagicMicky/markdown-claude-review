{
  "name": "markdown-claude-review",
  "displayName": "Markdown Claude Review",
  "description": "Google-Docs-style comment threads on markdown, wired back to Claude Code over MCP.",
  "version": "0.1.0",
  "publisher": "magicmicky",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/MagicMicky/markdown-claude-review.git"
  },
  "bugs": {
    "url": "https://github.com/MagicMicky/markdown-claude-review/issues"
  },
  "homepage": "https://github.com/MagicMicky/markdown-claude-review#readme",
  "keywords": [
    "markdown",
    "review",
    "comments",
    "annotations",
    "claude",
    "claude-code",
    "mcp",
    "documentation"
  ],
  "engines": {
    "vscode": "^1.90.0",
    "node": ">=18"
  },
  "categories": [
    "Other"
  ],
  "main": "./dist/extension.js",
  "bin": {
    "markdown-review-mcp": "./dist/mcp.js"
  },
  "activationEvents": [
    "onLanguage:markdown"
  ],
  "contributes": {
    "commands": [
      {
        "command": "mdreview.createThread",
        "title": "Add Comment",
        "category": "Markdown Review"
      },
      {
        "command": "mdreview.replyThread",
        "title": "Reply",
        "category": "Markdown Review"
      },
      {
        "command": "mdreview.commentOnSelection",
        "title": "Add Comment on Selection",
        "category": "Markdown Review",
        "icon": "$(comment-add)"
      },
      {
        "command": "mdreview.openPreviewToSide",
        "title": "Open Review Preview to the Side",
        "category": "Markdown Review",
        "icon": "$(open-preview)"
      },
      {
        "command": "mdreview.openPreview",
        "title": "Open Review Preview",
        "category": "Markdown Review",
        "icon": "$(preview)"
      },
      {
        "command": "mdreview.resolveThread",
        "title": "Resolve Thread",
        "category": "Markdown Review",
        "icon": "$(check)"
      },
      {
        "command": "mdreview.unresolveThread",
        "title": "Reopen Thread",
        "category": "Markdown Review",
        "icon": "$(history)"
      },
      {
        "command": "mdreview.deleteThread",
        "title": "Delete Thread",
        "category": "Markdown Review",
        "icon": "$(trash)"
      },
      {
        "command": "mdreview.reattachThread",
        "title": "Re-attach Stale Thread to Selection",
        "category": "Markdown Review",
        "icon": "$(link)"
      },
      {
        "command": "mdreview.revealThread",
        "title": "Go to Thread",
        "category": "Markdown Review"
      },
      {
        "command": "mdreview.nextThread",
        "title": "Next Comment",
        "category": "Markdown Review",
        "icon": "$(arrow-down)"
      },
      {
        "command": "mdreview.previousThread",
        "title": "Previous Comment",
        "category": "Markdown Review",
        "icon": "$(arrow-up)"
      },
      {
        "command": "mdreview.toggleInlineThreads",
        "title": "Toggle Inline Comment Threads",
        "category": "Markdown Review"
      },
      {
        "command": "mdreview.sendToClaude",
        "title": "Send Review to Claude Code",
        "category": "Markdown Review",
        "icon": "$(send)"
      },
      {
        "command": "mdreview.showResolved",
        "title": "Show Resolved Comments in Preview",
        "category": "Markdown Review",
        "icon": "$(eye)"
      },
      {
        "command": "mdreview.hideResolved",
        "title": "Hide Resolved Comments in Preview",
        "category": "Markdown Review",
        "icon": "$(eye-closed)"
      },
      {
        "command": "mdreview.setupClaude",
        "title": "Set Up Claude Code Integration",
        "category": "Markdown Review"
      },
      {
        "command": "mdreview.refresh",
        "title": "Refresh Threads",
        "category": "Markdown Review",
        "icon": "$(refresh)"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "mdreview.replyThread",
          "when": "false"
        },
        {
          "command": "mdreview.resolveThread",
          "when": "false"
        },
        {
          "command": "mdreview.unresolveThread",
          "when": "false"
        },
        {
          "command": "mdreview.deleteThread",
          "when": "false"
        },
        {
          "command": "mdreview.revealThread",
          "when": "false"
        },
        {
          "command": "mdreview.createThread",
          "when": "false"
        },
        {
          "command": "mdreview.reattachThread",
          "when": "false"
        },
        {
          "command": "mdreview.commentOnSelection",
          "when": "editorLangId == markdown"
        },
        {
          "command": "mdreview.nextThread",
          "when": "editorLangId == markdown"
        },
        {
          "command": "mdreview.previousThread",
          "when": "editorLangId == markdown"
        },
        {
          "command": "mdreview.openPreview",
          "when": "editorLangId == markdown"
        },
        {
          "command": "mdreview.openPreviewToSide",
          "when": "editorLangId == markdown"
        },
        {
          "command": "mdreview.showResolved",
          "when": "!mdreview.resolvedVisible"
        },
        {
          "command": "mdreview.hideResolved",
          "when": "mdreview.resolvedVisible"
        }
      ],
      "comments/commentThread/context": [
        {
          "command": "mdreview.createThread",
          "group": "inline@1",
          "when": "commentController == mdreview && commentThreadIsEmpty"
        },
        {
          "command": "mdreview.replyThread",
          "group": "inline@1",
          "when": "commentController == mdreview && !commentThreadIsEmpty"
        }
      ],
      "comments/commentThread/title": [
        {
          "command": "mdreview.resolveThread",
          "group": "navigation@1",
          "when": "commentController == mdreview && commentThread =~ /open|answered|stale/"
        },
        {
          "command": "mdreview.unresolveThread",
          "group": "navigation@2",
          "when": "commentController == mdreview && commentThread =~ /resolved/"
        },
        {
          "command": "mdreview.deleteThread",
          "group": "navigation@3",
          "when": "commentController == mdreview"
        }
      ],
      "editor/context": [
        {
          "command": "mdreview.commentOnSelection",
          "group": "1_modification@1",
          "when": "editorLangId == markdown"
        }
      ],
      "editor/title": [
        {
          "command": "mdreview.openPreviewToSide",
          "group": "navigation",
          "when": "editorLangId == markdown && mdreview.replacesPreview"
        },
        {
          "command": "mdreview.sendToClaude",
          "group": "navigation@2",
          "when": "activeWebviewPanelId == mdreview.preview"
        },
        {
          "command": "mdreview.showResolved",
          "group": "navigation@1",
          "when": "activeWebviewPanelId == mdreview.preview && !mdreview.resolvedVisible"
        },
        {
          "command": "mdreview.hideResolved",
          "group": "navigation@1",
          "when": "activeWebviewPanelId == mdreview.preview && mdreview.resolvedVisible"
        }
      ]
    },
    "configuration": {
      "title": "Markdown Claude Review",
      "properties": {
        "mdreview.reviewDir": {
          "type": "string",
          "default": ".review",
          "description": "Directory (relative to the workspace root) where comment threads and review requests are stored.",
          "scope": "machine"
        },
        "mdreview.author": {
          "type": "string",
          "default": "",
          "description": "Name recorded as the author of your comments. Defaults to your git user.name, else 'You'.",
          "scope": "machine"
        },
        "mdreview.terminalName": {
          "type": "string",
          "default": "claude",
          "description": "Substring used to find the Claude Code terminal when sending a review.",
          "scope": "machine"
        },
        "mdreview.highlightCommentedRanges": {
          "type": "boolean",
          "default": true,
          "description": "Underline commented passages in the editor, and highlight the one the cursor is in."
        },
        "mdreview.fuzzyThreshold": {
          "type": "number",
          "default": 0.62,
          "description": "Similarity (0-1) required to re-attach a comment to a rewritten paragraph. Below this the thread is marked stale instead."
        },
        "mdreview.sendPrompt": {
          "type": "string",
          "default": "/markdown-review",
          "description": "What the Send Review command types into the Claude Code terminal, before the scope. The document being reviewed is appended to it, so the hand-off names what it sent. Claude reads the comments over MCP, so this is just a trigger. Avoid names Claude Code already uses, such as /review, or this types something that silently does the wrong thing.",
          "scope": "machine",
          "markdownDescription": "What the Send Review command types into the Claude Code terminal, before the scope. The document being reviewed is appended to it — `/markdown-review docs/plan.md` — so the hand-off names what it sent instead of leaving Claude to work it out. Claude reads the comments over MCP, so this is just a trigger. Avoid names Claude Code already uses, such as `/review` — an unrecognised command is a visible error, but a colliding one silently does the wrong thing. Machine-scoped: it is typed into a terminal, so a workspace must not be able to set it."
        },
        "mdreview.inlineThreads": {
          "type": "string",
          "enum": [
            "collapsed",
            "expanded",
            "off"
          ],
          "default": "collapsed",
          "enumDescriptions": [
            "A gutter marker and an underline in the editor. Never displaces prose; click the marker to open a thread in place.",
            "Threads open inline in the editor, pushing the surrounding text down.",
            "No inline threads. The editor shows only an underline and a gutter marker, and the review preview is where you comment."
          ],
          "description": "How comment threads appear in the source editor. The review preview is unaffected."
        },
        "mdreview.showResolvedInPreview": {
          "type": "boolean",
          "default": false,
          "description": "List resolved comment threads in the review preview alongside the live ones. Off by default: the preview is the working surface, and resolved threads stay readable in the source editor's inline widget."
        },
        "mdreview.replaceBuiltInPreview": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Make the review preview *the* markdown preview: hide VS Code's own preview buttons from the editor title bar and take `Ctrl+K V` / `Ctrl+Shift+V`. Turn this off to get the built-in preview's buttons and keybindings back; the review preview is then on **Markdown Review: Open Review Preview** in the command palette."
        }
      }
    },
    "keybindings": [
      {
        "command": "mdreview.commentOnSelection",
        "key": "ctrl+alt+m",
        "mac": "cmd+alt+m",
        "when": "editorTextFocus && editorLangId == markdown"
      },
      {
        "command": "mdreview.openPreviewToSide",
        "key": "ctrl+k v",
        "mac": "cmd+k v",
        "when": "editorLangId == markdown && mdreview.replacesPreview"
      },
      {
        "command": "mdreview.openPreview",
        "key": "ctrl+shift+v",
        "mac": "cmd+shift+v",
        "when": "editorLangId == markdown && mdreview.replacesPreview"
      }
    ]
  },
  "scripts": {
    "build": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.webview.json",
    "test": "node --test dist-test/test/*.test.js dist-test/test/webview/*.test.js",
    "pretest": "node -e \"fs.rmSync('dist-test',{recursive:true,force:true})\" && tsc -p tsconfig.test.json && tsc -p tsconfig.webviewtest.json",
    "package": "npm run build && vsce package --no-dependencies -o markdown-claude-review.vsix"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "highlight.js": "^11.12.0",
    "markdown-it": "^15.0.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/jsdom": "^21.1.7",
    "@types/node": "^22.9.0",
    "@types/vscode": "^1.90.0",
    "@vscode/vsce": "^3.9.2",
    "esbuild": "^0.28.2",
    "jsdom": "^25.0.1",
    "ovsx": "^1.1.1",
    "typescript": "^5.6.3"
  },
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": false,
      "description": "Comment threads are read from files in the workspace, and the review hand-off types a configurable line into a terminal."
    }
  }
}
